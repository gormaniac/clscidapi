# The clscidapi config file

host = "0.0.0.0"
port = 80
log_level = 30
api_debug = false
clsc_ids_json = "~/.clscidapi/clsc_ids.json"

# ssl_certfile = ""
# ssl_keyfile = ""

# Eventually, we can fine tune uvicorn logging via this.
# [log_config]
